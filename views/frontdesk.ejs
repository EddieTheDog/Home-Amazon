<!DOCTYPE html>
<html>
<head>
  <title>Front Desk - Package Intake</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>📦 Front Desk - Create Package</h1>
  <form action="/frontdesk" method="POST">
    <input type="text" name="senderName" placeholder="Sender Name" required>
    <input type="text" name="recipientName" placeholder="Recipient Name" required>
    <input type="text" name="address" placeholder="Delivery Address" required>
    <textarea name="notes" placeholder="Special Instructions"></textarea>
    <button type="submit">Generate Package</button>
  </form>

  <% if (pkg) { %>
    <h2>✅ Package Created</h2>
    <p><strong>ID:</strong> <%= pkg.id %></p>
    <p><strong>Status:</strong> <%= pkg.status %></p>
    <img src="<%= qr %>" alt="QR Code">
    <p><a href="/track/<%= pkg.id %>">View Tracking Page</a></p>
  <% } %>
</body>
</html>
