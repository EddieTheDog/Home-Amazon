<!DOCTYPE html>
<html>
<head>
  <title>Front Desk - Create Package</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<h1>Front Desk - Create Package</h1>

<form action="/frontdesk/create" method="POST">
  <input name="recipient" placeholder="Recipient Name" required>
  <input name="address" placeholder="Delivery Address" required>
  <input name="city" placeholder="City" required>
  <input name="state" placeholder="State" required>
  <input name="zip" placeholder="ZIP" required>
  <input name="country" placeholder="Country" required>
  <input name="phone" placeholder="Phone" required>
  <input name="email" placeholder="Email" required>
  <input name="weight" placeholder="Package Weight" required>
  <input name="dimensions" placeholder="Dimensions (LxWxH)" required>
  <select name="speed">
    <option value="Standard">Standard</option>
    <option value="Priority">Priority</option>
  </select>
  <input name="deck" placeholder="Assign Shipping Deck (optional)">
  <textarea name="notes" placeholder="Special Notes"></textarea>
  <button type="submit">Create Package & Generate QR Code</button>
</form>

<% if (pkg) { %>
  <h2>Package Created!</h2>
  <p>ID: <%= pkg.id %></p>
  <p>Recipient: <%= pkg.recipient %></p>
  <p>Status: <%= pkg.status %></p>
  <img src="<%= pkg.qr %>" width="200">
<% } %>
</body>
</html>
