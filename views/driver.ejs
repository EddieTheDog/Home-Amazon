<!DOCTYPE html>
<html>
<head>
  <title>Driver Panel</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>🚚 Driver Panel</h1>

  <form action="/driver/lookup" method="POST">
    <input type="text" name="id" placeholder="Enter Package ID" required>
    <button type="submit">Lookup</button>
  </form>

  <% if (pkg) { %>
    <h2>Package Found</h2>
    <p><strong>ID:</strong> <%= pkg.id %></p>
    <p><strong>Recipient:</strong> <%= pkg.recipientName %></p>
    <p><strong>Address:</strong> <%= pkg.address %></p>
    <p><strong>Notes:</strong> <%= pkg.notes %></p>

    <form action="/driver/update/<%= pkg.id %>" method="POST" enctype="multipart/form-data">
      <input type="text" name="status" placeholder="New Status" required>
      <input type="file" name="photo" accept="image/*">
      <button type="submit">Update</button>
    </form>

    <% if (pkg.photo) { %>
      <img src="<%= pkg.photo %>" width="200">
    <% } %>
  <% } %>
</body>
</html>
