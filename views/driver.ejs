<!DOCTYPE html>
<html>
<head>
  <title>Driver Panel</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<h1>🚚 Driver Panel</h1>

<form action="/driver/lookup" method="POST">
  <input type="text" name="id" placeholder="Enter Package ID" required>
  <button type="submit">Lookup</button>
</form>

<% if (pkg) { %>
  <h2>Package Found</h2>
  <p>ID: <%= pkg.id %></p>
  <p>Recipient: <%= pkg.recipient %></p>
  <p>Address: <%= pkg.address %></p>
  <p>Status: <%= pkg.status %></p>
  <p>Notes: <%= pkg.notes %></p>

  <% if (pkg.status !== "Delivered") { %>
    <form action="/driver/claim/<%= pkg.id %>" method="POST">
      <button type="submit">Claim & Put On Truck</button>
    </form>

    <form action="/driver/deliver/<%= pkg.id %>" method="POST" enctype="multipart/form-data">
      <input type="file" name="photo" accept="image/*" required>
      <button type="submit">Deliver Package</button>
    </form>
  <% } %>

  <% if (pkg.photo) { %>
    <img src="<%= pkg.photo %>" width="200">
  <% } %>
<% } %>
</body>
</html>
